name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - uses: actions/checkout@v3
    
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
      
    - name: Run ESLint
      run: npm run lint
      
    - name: Run tests with dummy key
      run: npm test
      env:
        CI: true
        OPENAI_API_KEY: 'sk-dummy-key-for-testing'
        PROXY_TOKEN: 'test-token'

    - name: Run integration tests with real API
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: npm run test:integration
      env:
        CI: true
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        PROXY_TOKEN: ${{ secrets.PROXY_TOKEN }}

  validate:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: HTML5 Validator
      uses: Cyb3r-Jak3/html5validator-action@v7.2.0
      with:
        root: .
        format: text
        log_level: INFO
        
    - name: Check image sources
      run: |
        echo "Validating image URLs in HTML files..."
        grep -r "src=\"https:" . --include="*.html" | while read -r line ; do
          url=$(echo "$line" | grep -o 'https:[^"]*')
          code=$(curl -s -o /dev/null -w "%{http_code}" "$url" || echo "failed")
          if [ "$code" != "200" ]; then
            echo "Warning: Image $url returned status $code"
          fi
        done